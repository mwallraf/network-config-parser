Parse the configuration of the Carrier Ethernet devices.

Report the TDI information for each device. The config is parsed from local backup configs, not directly on a remote device.

Supports:

-> Ciena ERS/ESU parsing